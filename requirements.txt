# Runtime-зависимости. Python 3.12+ (тестируется на 3.14).
#
# ЕДИНЫЙ источник рантайм-зависимостей: build.py и scripts/launch.py ставят
# ровно этот файл (+ pyinstaller при сборке). flet пиним ТОЧНО — это весь UI,
# и «тестировали на 0.89, собрали 0.86.5» недопустимо. Обновление: поднять
# пин, прогнать pytest, собрать бинарь, только потом тег.
flet[web]==0.86.5
packaging>=23.0,<27       # сравнение версий по PEP 440 в self-updater
certifi>=2024.2.2         # CA-бандл для HTTPS-запросов self-updater из frozen-сборки
work-calendar==1.1.0      # производственный календарь РФ
pdfplumber>=0.11,<0.12    # опционально: парсер PDF-поправок к календарю

# Dev / CI (не бандлится PyInstaller):
#   pytest pytest-cov ruff
